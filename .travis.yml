language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-code' ]; then cd .. && eval "mv $currentfolder generator-code" && cd generator-code; fi
